# SecureAssess API

FastAPI backend for the SecureAssess platform.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
```

## Run

```bash
uvicorn main:app --reload
```

API docs available at http://localhost:8000/docs
