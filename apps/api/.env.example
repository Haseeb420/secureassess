ENVIRONMENT=development

SUPABASE_URL=
SUPABASE_SERVICE_KEY=
SUPABASE_JWT_SECRET=

# ── Database ─────────────────────────────────────────────────────────────────
# Option A — local PostgreSQL (native psql, after running: make db-setup)
DATABASE_URL=postgresql://secureassess:secureassess@localhost:5432/secureassess
# Option B — local Docker PostgreSQL (after running: make db-setup-docker)
# DATABASE_URL=postgresql://secureassess:secureassess@localhost:5432/secureassess
# Option C — Supabase cloud
# DATABASE_URL=postgresql://postgres:[password]@db.[project].supabase.co:5432/postgres

JUDGE0_URL=
JUDGE0_API_KEY=
ENCRYPTION_SECRET=
JWT_SECRET=
